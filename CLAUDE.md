# EduVoice — контекст проекта для ИИ-ассистента

Этот файл — точка входа для любой модели, которая открывает репозиторий впервые.
Claude Code читает его автоматически; другому ассистенту его достаточно дать целиком.
Всё написанное ниже проверено на живом сервере, а не предполагается.

Дата последней правки: 19.09.2026.

---

## 1. Что это

**EduVoice** — голосовой ИИ-оператор call-центра Министерства высшего образования
Узбекистана плюс CRM для сотрудников. Делается на хакатоне «Umummilliy AI Xakaton»
(Хорезм, 17–20.09.2026), задачи №5 (автоматизация call-центра с узбекской речью) и
№3 (ответы строго по нормативным документам).

Человек звонит → ассистент слушает, распознаёт, отвечает по регламенту
или переводит на живого оператора → разговор, расшифровка и решение попадают в CRM.

Проект делает один человек. Модуль VoiceLab первоначально планировался на второго
программиста, но тот выбыл, и речь подключили сами 18.09.

## 2. Состояние на сегодня

| Часть | Состояние |
|---|---|
| Телефония (FreePBX/Asterisk), очередь операторов | ✅ работает на сервере |
| Мост: диалог, перебивание, переводы, журнал, запись | ✅ работает |
| CRM: звонки, обращения, граждане, база знаний, аналитика, роли | ✅ работает |
| База знаний CRM управляет речью ассистента без перезапуска | ✅ проверено живыми звонками |
| Настоящая узбекская речь (VoiceLab) | ✅ модуль `eduvoice/voicelab.py`, проверен живыми звонками |
| Ответы FAQ сверены с lex.uz | ✅ шесть ответов с номерами документов и пунктов |
| Пороги детектора речи на живой речи | ✅ проверены на синтезированной узбекской речи через телефонный канал |
| Кредиты VoiceLab | ⚠️ закончились: синтез отдаёт 402, распознавание продолжает работать |
| Коммиты в git | ✅ история есть, репозиторий публичный: `dismoilov/eduvoice` |

192 тестов, `ruff` + `mypy` чисто. Скорость страниц — в [docs/AUDIT.md](docs/AUDIT.md).

## 3. Как всё устроено

```
Звонок → Asterisk (7000) → [eduvoice-ai]
                                 │ POST /calls/{uuid}/start          (HTTP 9093)
                                 │ MixMonitor → запись .wav
                                 │ Dial(AudioSocket/127.0.0.1:9092/{uuid})  ← звук 8 кГц
                                 │ GET /calls/{uuid}/next → operator | hangup
                                 ▼
                       мост eduvoice-bridge (Python)
                                 │ пишет звонок, реплики, гражданина
                                 ▼
                    SQLite /opt/eduvoice/data/eduvoice.db  (WAL)
                                 ▲
                                 │ читает всё, правит; публикует ответы
                       CRM eduvoice-crm (FastAPI, порт 9095)
```

Два процесса намеренно раздельны: перезапуск CRM не может уронить разговор. Общий у них
только файл базы. Мост читает из базы **только опубликованные** ответы, не чаще раза в
10 секунд.

## 4. Карта репозитория

```
eduvoice/          мост (один звонок = одно TCP-соединение AudioSocket)
  main.py          запуск, выбор провайдеров, прогрев фраз
  session.py       конечный автомат звонка: greeting→listening→thinking→speaking→closing
  audiosocket.py   протокол Asterisk + ровная подача звука кадрами по 20 мс
  vad.py           границы фразы и перебивание (webrtcvad)
  audio.py         PCM: ресемплинг (StreamResampler), нарезка кадров
  brain.py         решение по вопросу: правила поверх модели
  content.py       FAQ (из базы или из content/faq.yaml)
  prompts.py       служебные фразы + кэш синтеза на диске
  control_api.py   HTTP для диалплана (три ручки, без зависимостей)
  registry.py      состояние звонков в памяти, DEFAULT_NEXT = "operator"
  calllog.py       резервный JSON-журнал (logs/*.jsonl)
  config.py        все настройки из .env
  interfaces.py    контракт провайдеров (SpeechToText / TextToSpeech / ChatModel)
  fakes.py         заглушки провайдеров
  voicelab.py      настоящие речь и модель (VoiceLab.uz); переключатель EDUVOICE_PROVIDER
  voice_tools.py   озвучить фразу, разово наполнить кэш (prewarm)
store/             общая база
  db.py            схема, миграции (PRAGMA user_version), WAL
  write.py         запись звонка (мост), импорт старых журналов
  knowledge.py     опубликованные ответы для моста, кэш 10 с
crm/               рабочее место call-центра
  app.py repo.py stats.py security.py deps.py i18n.py telephony.py cli.py
  views/           экраны, templates/ страницы, static/ стиль и плеер
asterisk/          диалплан, глобальные переменные, очередь — исходник правды
content/           prompts.yaml (служебные фразы), faq.yaml (резервная копия ответов)
deploy/            systemd-юниты обоих сервисов
docs/              документация (см. ниже)
tests/             192 тестов
```

## 5. Инварианты — не нарушать

1. **Звонящий никогда не остаётся в тишине.** Любая ошибка → перевод на оператора.
   Решение для диалплана пишется в реестр **до** всего, что может упасть; control API на
   неизвестный звонок отвечает `operator`; диалплан использует `Dial(AudioSocket/…,,g)`,
   а не приложение `AudioSocket()` (оно кладёт трубку при падении моста).
2. **Модель не распоряжается звонком.** Она возвращает только JSON с intent; что делать —
   решают правила в `brain.py`. Выдуманный `faq_id` не озвучивается, модель не может сама
   положить трубку.
3. **Черновик ответа не попадает к гражданину.** Мост читает только `status='published'`.
4. **Форматы звука зафиксированы:** телефония 8 кГц, распознавание 16 кГц, синтез 24 кГц;
   пересчёт делает мост, провайдеры его не касаются.
5. **Ошибки провайдеров** приходят как `SttError`/`TtsError`/`LlmError`, иначе мост падает.
6. **Секретов в репозитории нет.** Всё в `.env` на сервере (права 600) и в
   `/root/eduvoice-credentials.txt`.
7. **Перед выкладкой — зелёные тесты:** `make deploy` сам запускает `make check`.
8. **В CRM любой сотрудник видит любой звонок и любого гражданина** — это решение, а не
   упущение: оператор отвечает тому, кто позвонил. Ограничены только разделы (аналитика —
   супервайзеру, администрирование — администратору) и правка базы знаний. Подотчётность
   держится на журнале действий.
9. **Аудио-кэш (`audio/tts-cache`, `audio/prompts`) не удалять.** Это оплаченный синтез:
   каждая фраза стоит кредитов, а файлы вечны. `make deploy` их не трогает — и правильно.
   Восстановить можно только заново заплатив.
10. **Элементы управления в CRM имеют одну высоту** (`--control-h` в `crm/static/app.css`),
   в шаблонах не должно появляться `style=` для вёрстки — только для вычисляемых значений
   вроде высоты столбика графика.

## 6. Решения и их причины (не «чинить» обратно)

| Решение | Причина |
|---|---|
| **Меню убрано, 7000 сразу к помощнику** | предложив выбор «1 — помощник, 0 — оператор», гарантированно получаешь нажатие 0 по привычке; помощника никто не услышит. Попросить человека можно голосом |
| **Приветствие нельзя перебить** (`INTERRUPTIBLE_GREETING`) | оно объясняет, что это за служба; в шумном зале обрывалось шумом за полсекунды, и звонящий не успевал понять, куда попал |
| **Порог громкости поверх webrtcvad** (`LOUDNESS_MARGIN` × `LOUDNESS_FLOOR_MIN` = 300) | webrtcvad в зале называет речью 70% кадров и даже чистую тишину; без порога фраза не может закончиться никогда |
| **Телефонам разрешены только ulaw/alaw** (`pjsip.endpoint_custom_post.conf`) | софтфон возьмёт G.722, а у него та же скорость, что у G.711 — мост раскодирует его как ulaw и слышит рёв |
| **Диалплан шлёт `CHANNEL(audionativeformat)`** | `audioreadformat` показывает формат уже после преобразования и говорит «ulaw», когда телефон шлёт G.722 |
| **Звонки дописываются по SIGTERM** | `make deploy` перезапускает мост; без этого идущие разговоры пропадали и из базы, и из резервного журнала |
| AudioSocket, а не ARI | нужен только звук; AudioSocket встроен в Asterisk 20 |
| `Dial(AudioSocket/…)` вместо `AudioSocket()` | проверено: при падении моста звонок переживает и уходит оператору |
| webrtcvad | единственный VAD, который ставится на Sangoma Linux 7 (glibc 2.17) |
| numpy<2.3, soxr<1 | нет колёс под glibc 2.17 |
| SQLite (WAL) | одна машина, нужны транзакции и поиск; FTS5 для расшифровок |
| CRM на FastAPI + Jinja2, серверный рендеринг | десятки форм; никакой сборки и npm — на защите может не быть интернета |
| control API без зависимостей | три ручки, а новые колёса на старом сервере — риск |
| Пароли `hashlib.scrypt`, сессии `hmac` | стандартная библиотека, нечего ставить |
| Плоская раскладка пакетов | при `src/` uv периодически терял модуль |
| Ответы живут в базе, а не в YAML | супервайзер правит текст без программиста |

## 7. Сервер

```
10.103.10.86   Sangoma Linux 7, FreePBX 16, Asterisk 20.4
```

Доступ по ssh под `root`; пароль и все остальные секреты — у человека A и в
`/root/eduvoice-credentials.txt` на самом сервере. **В репозитории секретов нет и быть
не должно.**

| Что | Где |
|---|---|
| Код | `/opt/eduvoice/app`, пользователь `eduvoice` |
| База | `/opt/eduvoice/data/eduvoice.db` |
| Записи разговоров | `/var/spool/asterisk/monitor/eduvoice/<дата>/<uuid>.wav` |
| Журналы звонков | `/opt/eduvoice/app/logs/<дата>.jsonl` |
| Сервисы | `eduvoice-bridge`, `eduvoice-crm` |
| Порты | 9092 звук, 9093 control API (оба только 127.0.0.1), 9095 CRM (по сети) |
| Секреты | `/root/eduvoice-credentials.txt` |
| Диалплан | `/etc/asterisk/extensions_custom.conf`, `globals_custom.conf`, `queues_custom.conf` |
| Внутренние номера | 7000 ассистент, 300 операторы, 7777 эхо-тест |
| Контексты | `eduvoice-ai`, `eduvoice-operators`, `eduvoice-test`, `eduvoice-demo`, `eduvoice-testagent` |

## 8. Команды

```bash
make check                    # линтер + типы + тесты (обязательно перед выкладкой)
make deploy                   # выкатить оба сервиса
make deploy-dialplan          # выкатить диалплан, глобальные переменные и очередь
make logs / make crm-logs     # живые логи
make status                   # сервисы, порты, номера
make say TEXT="…"             # что «услышит» заглушка распознавания (нужен перезапуск моста)
make call-test                # Asterisk сам звонит ассистенту
make barge-test               # звонок с перебиванием
make agent-on                 # подставной оператор в очередь
make calls                    # последние звонки с задержками
make backup                   # копия базы
make sql Q="select …"         # заглянуть в базу (системный sqlite3 её не читает)
make voice-prewarm            # озвучить все фразы разово (ARGS=--dry-run — только счёт)
make crm-user USER_LOGIN=… USER_PASSWORD=… USER_ROLE=operator
```

Звонок с номером (чтобы в CRM появился гражданин):

```bash
asterisk -rx "channel originate Local/998901234567@eduvoice-demo/n extension qhello-world@eduvoice-test"
```

## 9. Как проверять, а не гадать

- **Живой звонок:** `make say TEXT="stipendiya qanday olinadi"` → `make call-test` →
  `make logs` (видно `heard`, `decision`, задержки) → CRM → Звонки.
- **База:** `make sql Q="select …"`. Системный `sqlite3` на сервере — версии 3.7.17 от
  2013 года, он не знает ни FTS5, ни индексов по выражениям и на исправную базу отвечает
  `malformed database schema`. Приложение работает с той же базой через SQLite 3.53 из
  Python проекта; `make sql` использует именно его и разрешает только чтение.
- **Сколько стоит озвучка:** `make voice-prewarm ARGS=--dry-run` — считает, не тратя.
  1 кредит VoiceLab = 1 знак синтеза; распознавание — 400 кредитов за минуту.
- **CRM снаружи:** сначала забрать токен со страницы входа (`name="csrf"`), потом
  `curl -s -b jar -c jar --data-urlencode "login=…" --data-urlencode "password=…"
  --data-urlencode "csrf=$CSRF" http://10.103.10.86:9095/login`, дальше с `-b jar`.
- **Отказоустойчивость:** остановить `eduvoice-bridge` во время звонка — звонок обязан
  уйти в очередь операторов.
- **Скриншоты интерфейса:** локально поднять CRM и снять headless-браузером — так уже
  ловились ошибки вёрстки, которых не видно в HTML.

## 10. Соглашения

- **Комментарии и docstring в коде — по-английски**, документация и общение с
  пользователем — по-русски, интерфейсы для людей — узбекский + русский.
- Узбекская орфография: `oʻ`/`gʻ` — U+02BB, tutuq belgisi — U+02BC. В `pyproject.toml`
  они внесены в `allowed-confusables`, менять на апострофы нельзя.
- **`javob` по-узбекски значит «ответ»** — это слово встречается в текстах ассистента и в
  подписях CRM. Не переименовывать: проект назывался Javob до 18.09, но теперь EduVoice
  только там, где это идентификатор.
- Тесты пишутся так, чтобы падать на сломанном коде: у каждого — конкретный сценарий
  отказа, а не «функция вернула что-то».
- Пользователь просил: проверять каждый шаг вживую, не изобретать факты, не торопиться с
  гитом, интерфейсы делать непохожими на типовые ИИ-шаблоны.

## 11. Что осталось

Функционально — ничего: A9, A11 и A13 закрыты и проверены живыми звонками.

Единственный блокер внешний: **на аккаунте VoiceLab кончились кредиты**. Распознавание и
модель работают, синтез отдаёт `HTTP 402 insufficient_credits`, и звонок уходит оператору
с исходом `tech_problem`. Нужно 1 674 кредита, чтобы озвучить оставшиеся 13 фраз один раз
(`make voice-prewarm`), плюс примерно 130 кредитов на каждый демонстрационный звонок —
это распознавание, синтез после наполнения кэша бесплатен.

## 12. Документация

| Файл | О чём |
|---|---|
| `README.md` | обзор проекта |
| `docs/ARCHITECTURE.md` | как устроено и почему |
| `docs/CRM.md` | экраны CRM, роли, связка базы знаний с речью ассистента |
| `docs/REQUIREMENTS.md` | требования задания и доказательство каждого — для проверяющего |
| `docs/CHECKS.md` | проверка всей системы за 10 минут, с ожидаемым выводом |
| `docs/DATA.md` | данные о гражданине: что хранится, кто видит, чем защищено |
| `docs/OPERATIONS.md` | сервер, деплой, диагностика, приватность |
| `docs/AUDIT.md` | независимый аудит кода и что исправлено |
| `docs/VOICELAB-TASKS.md` | разбор API VoiceLab: обещанное документацией против фактического |
| `PLAN.md`, `TASKS.md` | исходный план и маршрут работ с отметками о проверке |
